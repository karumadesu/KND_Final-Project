package tiles;

import java.awt.image.*;

public class GameTile{
    public BufferedImage image; // Stores a tile's image data
    public boolean isColliding = false; // Stores a tile's collision state
}
